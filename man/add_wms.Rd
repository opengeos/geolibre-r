% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_wms}
\alias{add_wms}
\title{Add a WMS layer}
\usage{
add_wms(
  map,
  endpoint,
  layers,
  name = "WMS Layer",
  styles = "",
  image_format = "image/png",
  transparent = TRUE,
  tile_size = 256,
  version = "1.1.1",
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{endpoint}{WMS service endpoint, the \code{GetMap} base URL.}

\item{layers}{Comma-separated WMS layer name(s).}

\item{name}{Layer name.}

\item{styles}{Comma-separated WMS style name(s); empty for the server default.}

\item{image_format}{WMS image format, for example \code{"image/png"}.}

\item{transparent}{Whether to request transparent tiles.}

\item{tile_size}{Tile size in pixels.}

\item{version}{WMS protocol version, \code{"1.1.1"} or \code{"1.3.0"}. Version 1.3.0
sends \code{CRS} instead of \code{SRS}; some servers accept only one version.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
The layer is rendered as tiled raster from WMS \code{GetMap} requests, built
exactly as the application's Add Data dialog builds them.
}
\examples{
map <- geolibre() |>
  add_wms(
    "https://example.com/geoserver/wms",
    layers = "topp:states",
    name = "States"
  )
stopifnot(map$x$project$layers[[1]]$type == "wms")
}
