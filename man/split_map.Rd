% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{split_map}
\alias{split_map}
\title{Add a split-map comparison slider}
\usage{
split_map(
  map,
  left_layers = NULL,
  right_layers = NULL,
  orientation = c("vertical", "horizontal"),
  position = 50,
  control_position = c("top-left", "top-right", "bottom-left", "bottom-right")
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{left_layers}{Layer ids or names shown on the left or top of the slider.
The string \code{"__basemap__"} selects the basemap.}

\item{right_layers}{Layer ids or names shown on the right or bottom.}

\item{orientation}{\code{"vertical"} to move the slider left and right, or
\code{"horizontal"} to move it up and down.}

\item{position}{Initial slider position as a percentage from 0 to 100.}

\item{control_position}{Corner for the swipe panel: \code{"top-left"},
\code{"top-right"}, \code{"bottom-left"}, or \code{"bottom-right"}.}
}
\value{
The modified widget.
}
\description{
Enables the Layer Swipe control, which clips one set of layers to one side of
a draggable slider and another set to the other, for before-and-after
comparisons.
}
\examples{
map <- geolibre() |>
  add_raster("https://example.com/before.tif", name = "Before") |>
  add_raster("https://example.com/after.tif", name = "After") |>
  split_map("Before", "After")
stopifnot(length(map$x$project$plugins$settings) == 1L)
}
