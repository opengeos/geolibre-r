% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_pmtiles}
\alias{add_pmtiles}
\title{Add a PMTiles layer}
\usage{
add_pmtiles(
  map,
  url,
  name = "PMTiles",
  tile_type = c("vector", "raster"),
  source_layers = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{url}{URL of the \code{.pmtiles} archive.}

\item{name}{Layer name.}

\item{tile_type}{\code{"vector"} or \code{"raster"}.}

\item{source_layers}{Vector source layer names to render. Vector tiles only.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
The application registers the \verb{pmtiles://} protocol itself, so a plain
\verb{https://} URL to the archive is what this expects.
}
\examples{
map <- geolibre() |>
  add_pmtiles("https://example.com/data.pmtiles", source_layers = "buildings")
stopifnot(map$x$project$layers[[1]]$type == "pmtiles")
}
