% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{add_legend}
\alias{add_legend}
\title{Add a legend to the map}
\usage{
add_legend(
  map,
  title = NULL,
  legend = NULL,
  labels = NULL,
  colors = NULL,
  builtin = NULL,
  position = c("bottom-left", "bottom-right", "top-left", "top-right"),
  shape = c("square", "circle", "line")
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{title}{Legend title. Defaults to \code{"Legend"}, or the preset's own title
when \code{builtin} is supplied without one.}

\item{legend}{A named vector or list mapping label to CSS color. Order is
preserved.}

\item{labels}{Item labels, paired position-wise with \code{colors}.}

\item{colors}{Item CSS colors, paired position-wise with \code{labels}.}

\item{builtin}{A built-in preset name from \code{\link[=builtin_legend_names]{builtin_legend_names()}}.}

\item{position}{Corner for the legend: \code{"top-left"}, \code{"top-right"},
\code{"bottom-left"}, or \code{"bottom-right"}.}

\item{shape}{Swatch shape for every item: \code{"square"}, \code{"circle"}, or \code{"line"}.}
}
\value{
The modified widget.
}
\description{
Supply the legend entries exactly one of three ways: a built-in preset
(\code{builtin}), a named vector or list of label-to-color pairs (\code{legend}), or
parallel \code{labels} and \code{colors} vectors. Each call adds another legend, so a
map can carry several at once.
}
\examples{
map <- geolibre() |>
  add_legend(
    "Land cover",
    legend = c(Water = "#466b9f", Forest = "#1c5f2c"),
    position = "bottom-left"
  )

# A built-in preset carries its own title and colors.
geolibre() |> add_legend(builtin = "nlcd")
}
\seealso{
\code{\link[=add_colorbar]{add_colorbar()}} for continuous rasters, \code{\link[=builtin_legend_names]{builtin_legend_names()}}
}
