% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{geolibre_command}
\alias{geolibre_command}
\title{Send a command to a live GeoLibre map}
\usage{
geolibre_command(proxy, method, params = list(), request_id = NULL)
}
\arguments{
\item{proxy}{A GeoLibre proxy created by \code{\link[=geolibre_proxy]{geolibre_proxy()}}.}

\item{method}{The scripting method name, for example \code{"flyTo"} or \code{"toImage"}.}

\item{params}{Named list of parameters for the method.}

\item{request_id}{Optional id echoed back with the reply, so several in-flight
commands can be told apart. Generated when omitted.}
}
\value{
The proxy, invisibly.
}
\description{
The escape hatch behind the \verb{geolibre_*()} command functions: it forwards any
method the application's scripting bridge implements. The reply arrives
asynchronously on the \code{input$id_result} input described in \code{\link[=geolibreOutput]{geolibreOutput()}}.
}
\examples{
if (interactive() && requireNamespace("shiny", quietly = TRUE)) {
  geolibre_command(geolibre_proxy("map"), "zoomToLayer", list(layerId = "abc"))
}
}
\seealso{
\code{\link[=geolibre_fly_to]{geolibre_fly_to()}}, \code{\link[=geolibre_get_view]{geolibre_get_view()}}, \code{\link[=geolibre_run_algorithm]{geolibre_run_algorithm()}}
}
