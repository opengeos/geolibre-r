% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{geolibre_layer_features}
\alias{geolibre_layer_features}
\alias{geolibre_selected_features}
\alias{geolibre_drawn_features}
\title{Read features from a live GeoLibre map}
\usage{
geolibre_layer_features(proxy, layer_id, request_id = NULL)

geolibre_selected_features(proxy, request_id = NULL)

geolibre_drawn_features(proxy, request_id = NULL)
}
\arguments{
\item{proxy}{A GeoLibre proxy created by \code{\link[=geolibre_proxy]{geolibre_proxy()}}.}

\item{layer_id}{The layer's id, as reported by \code{\link[=get_layers]{get_layers()}}.}

\item{request_id}{Optional id echoed back with the reply.}
}
\value{
The proxy, invisibly.
}
\description{
\code{geolibre_layer_features()} returns one layer's features,
\code{geolibre_selected_features()} the current selection, and
\code{geolibre_drawn_features()} whatever the user drew with the map's editing
tools. Each reply arrives on \code{input$id_result}.
}
\examples{
if (interactive() && requireNamespace("shiny", quietly = TRUE)) {
  geolibre_drawn_features(geolibre_proxy("map"))
}
}
