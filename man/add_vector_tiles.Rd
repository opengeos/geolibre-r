% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_vector_tiles}
\alias{add_vector_tiles}
\title{Add a vector tile layer from a TileJSON endpoint}
\usage{
add_vector_tiles(
  map,
  url,
  name = "Vector Tiles",
  source_layers = NULL,
  source_layer = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{url}{TileJSON endpoint for the vector tileset.}

\item{name}{Layer name.}

\item{source_layers}{Source layer names to render, for multi-layer tilesets.}

\item{source_layer}{A single source layer name, for the common single-layer
case. Ignored when \code{source_layers} is supplied.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Add a vector tile layer from a TileJSON endpoint
}
\examples{
map <- geolibre() |>
  add_vector_tiles("https://example.com/tiles.json", source_layer = "roads")
stopifnot(map$x$project$layers[[1]]$type == "vector-tiles")
}
