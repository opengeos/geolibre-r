% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny.R
\name{geolibre_to_image}
\alias{geolibre_to_image}
\title{Capture a live GeoLibre map as a PNG}
\usage{
geolibre_to_image(proxy, request_id = NULL)
}
\arguments{
\item{proxy}{A GeoLibre proxy created by \code{\link[=geolibre_proxy]{geolibre_proxy()}}.}

\item{request_id}{Optional id echoed back with the reply.}
}
\value{
The proxy, invisibly.
}
\description{
The reply arrives on \code{input$id_result} with a \code{value} holding a
\verb{data:image/png;base64,...} URL. Strip the prefix up to the comma and pass the
rest to \code{\link[jsonlite:base64_dec]{jsonlite::base64_dec()}} to recover the PNG bytes.
}
\examples{
if (interactive() && requireNamespace("shiny", quietly = TRUE)) {
  geolibre_to_image(geolibre_proxy("map"))
}
}
