% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_marker_cluster}
\alias{add_marker_cluster}
\title{Add clustered point markers}
\usage{
add_marker_cluster(
  map,
  points,
  name = "Marker Cluster",
  cluster_radius = 50,
  cluster_max_zoom = 14,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{points}{Points as a two-column matrix or a data frame of coordinates, a
list of \code{c(longitude, latitude)} pairs or named
\code{list(lng = , lat = , ...)} entries, a point GeoJSON source, or an \code{sf}
object of points.}

\item{name}{Layer name.}

\item{cluster_radius}{Cluster radius in pixels.}

\item{cluster_max_zoom}{Zoom level beyond which points are no longer clustered.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Builds a point layer with the cluster renderer enabled, so nearby points
collapse into count bubbles that split apart as the map zooms in.
}
\examples{
map <- geolibre() |>
  add_marker_cluster(list(c(-77, 39), c(-77.1, 39.1)), cluster_radius = 60)
stopifnot(map$x$project$layers[[1]]$style$pointRenderer == "cluster")
}
