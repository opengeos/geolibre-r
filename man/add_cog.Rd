% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_cog}
\alias{add_cog}
\title{Add a Cloud Optimized GeoTIFF layer}
\usage{
add_cog(
  map,
  url,
  name = "COG",
  bands = NULL,
  colormap = NULL,
  rescale = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{url}{Public HTTP(S) URL of a Cloud Optimized GeoTIFF or GeoTIFF.}

\item{name}{Layer name.}

\item{bands}{Optional one-based band indices. Three or more bands render as
RGB; one renders as a single band, which \code{colormap} then colors.}

\item{colormap}{Optional GeoLibre colormap name for single-band rendering.}

\item{rescale}{Optional list of numeric \code{c(min, max)} ranges, one per rendered
band. A single \code{c(min, max)} pair is accepted for the one-band case.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Add a Cloud Optimized GeoTIFF layer
}
\examples{
map <- geolibre() |>
  add_cog("https://example.com/image.tif", bands = c(1, 2, 3))
stopifnot(map$x$project$layers[[1]]$type == "cog")
}
\seealso{
\code{\link[=add_raster]{add_raster()}}, \code{\link[=add_tile_layer]{add_tile_layer()}}, \code{\link[=add_colorbar]{add_colorbar()}}
}
