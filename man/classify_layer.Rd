% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-manage.R
\name{classify_layer}
\alias{classify_layer}
\title{Symbolize an existing layer as a choropleth}
\usage{
classify_layer(
  map,
  layer,
  column,
  class_count = 5,
  colormap = "viridis",
  scheme = c("equal-interval", "quantile")
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{layer}{A layer id or layer name. The layer must carry inlined GeoJSON.}

\item{column}{Name of the numeric feature property to classify.}

\item{class_count}{Number of classes, clamped to between 2 and 12.}

\item{colormap}{A color ramp name from \code{\link[=color_ramp_names]{color_ramp_names()}}.}

\item{scheme}{Classification scheme, \code{"equal-interval"} or \code{"quantile"}.}
}
\value{
The modified widget.
}
\description{
Symbolize an existing layer as a choropleth
}
\examples{
counties <- list(
  type = "FeatureCollection",
  features = list(
    list(
      type = "Feature", properties = list(pop = 10),
      geometry = list(type = "Point", coordinates = c(-77, 39))
    ),
    list(
      type = "Feature", properties = list(pop = 90),
      geometry = list(type = "Point", coordinates = c(-76, 40))
    )
  )
)
map <- geolibre() |>
  add_geojson(counties, name = "Counties") |>
  classify_layer("Counties", "pop", colormap = "reds")
stopifnot(map$x$project$layers[[1]]$style$vectorStyleProperty == "pop")
}
\seealso{
\code{\link[=add_choropleth]{add_choropleth()}} to add and symbolize in one call.
}
