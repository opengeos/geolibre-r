% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_csv}
\alias{add_csv}
\title{Add a CSV of point coordinates}
\usage{
add_csv(
  map,
  data,
  x = "longitude",
  y = "latitude",
  name = "CSV",
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{data}{A data frame, a CSV file path, a CSV URL, CSV text, or a list of
row lists.}

\item{x}{Name of the longitude column.}

\item{y}{Name of the latitude column.}

\item{name}{Layer name.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
\code{\link[=add_xy_data]{add_xy_data()}} with a CSV-oriented default layer name.
}
\examples{
text <- "name,longitude,latitude\nDC,-77.0369,38.9072"
map <- geolibre() |> add_csv(text)
}
