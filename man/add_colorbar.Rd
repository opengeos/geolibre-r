% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{add_colorbar}
\alias{add_colorbar}
\title{Add a colorbar to the map}
\usage{
add_colorbar(
  map,
  colormap = "viridis",
  vmin = 0,
  vmax = 1,
  label = "",
  units = "",
  colors = NULL,
  orientation = c("vertical", "horizontal"),
  position = c("bottom-right", "bottom-left", "top-left", "top-right")
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{colormap}{A color ramp name from \code{\link[=color_ramp_names]{color_ramp_names()}}. Ignored when
\code{colors} is supplied.}

\item{vmin}{Value at the low end of the colorbar.}

\item{vmax}{Value at the high end of the colorbar.}

\item{label}{Title shown alongside the colorbar.}

\item{units}{Units suffix shown with the values.}

\item{colors}{Optional character vector of CSS colors defining a custom
gradient, used instead of \code{colormap}.}

\item{orientation}{\code{"vertical"} or \code{"horizontal"}.}

\item{position}{Corner for the colorbar: \code{"top-left"}, \code{"top-right"},
\code{"bottom-left"}, or \code{"bottom-right"}.}
}
\value{
The modified widget.
}
\description{
Renders a gradient with minimum and maximum ticks, from either a named color
ramp or an explicit list of CSS colors. Each call adds another colorbar.
}
\examples{
map <- geolibre() |>
  add_raster("https://example.com/dem.tif", bands = 1, colormap = "terrain") |>
  add_colorbar(
    colormap = "terrain", vmin = 0, vmax = 3000,
    label = "Elevation", units = "m"
  )
}
\seealso{
\code{\link[=add_legend]{add_legend()}} for categorical classes
}
