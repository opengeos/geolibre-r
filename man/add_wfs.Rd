% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_wfs}
\alias{add_wfs}
\title{Add a WFS layer}
\usage{
add_wfs(
  map,
  endpoint,
  type_name,
  name = "WFS Layer",
  version = "2.0.0",
  output_format = "application/json",
  srs_name = "EPSG:4326",
  max_features = 1000,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{endpoint}{WFS service endpoint.}

\item{type_name}{WFS feature type name, for example \code{"topp:states"}.}

\item{name}{Layer name.}

\item{version}{WFS protocol version, for example \code{"2.0.0"} or \code{"1.1.0"}.}

\item{output_format}{Requested output format; must yield GeoJSON.}

\item{srs_name}{Spatial reference of the response.}

\item{max_features}{Cap on the number of returned features, since the response
is inlined. Pass \code{NULL} to request every feature.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
The \code{GetFeature} response is fetched and inlined into the project, so the
endpoint must be able to return GeoJSON. This function contacts the service
when called.
}
\examples{
\dontrun{
geolibre() |>
  add_wfs("https://ahocevar.com/geoserver/wfs", type_name = "topp:states")
}
}
