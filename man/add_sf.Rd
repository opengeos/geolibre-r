% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_sf}
\alias{add_sf}
\title{Add an \code{sf} object to a GeoLibre map}
\usage{
add_sf(
  map,
  data,
  name = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{data}{An \code{sf}, \code{sfc}, or \code{sfg} object.}

\item{name}{Layer name. Defaults to the expression passed as \code{data}.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
The object is transformed to EPSG:4326 before serialization. An object with no
CRS is taken to be in longitude/latitude order already, since that is what
GeoJSON means.
}
\examples{
if (requireNamespace("sf", quietly = TRUE)) {
  point <- sf::st_sf(
    name = "Washington, DC",
    geometry = sf::st_sfc(sf::st_point(c(-77.0369, 38.9072)), crs = 4326)
  )
  map <- geolibre() |> add_sf(point, name = "Places")
}
}
