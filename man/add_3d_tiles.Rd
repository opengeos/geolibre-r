% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_3d_tiles}
\alias{add_3d_tiles}
\title{Add a 3D Tiles layer}
\usage{
add_3d_tiles(
  map,
  url,
  name = "3D Tiles",
  altitude_offset = 0,
  request_headers = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{url}{URL of the 3D Tiles \code{tileset.json}.}

\item{name}{Layer name.}

\item{altitude_offset}{Vertical offset applied to the tileset, in meters.}

\item{request_headers}{Optional named list of request headers. These are
stored in the project, so avoid persisting secrets; \code{\link[=save_project]{save_project()}} strips
them unless \code{keep_credentials = TRUE}.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Add a 3D Tiles layer
}
\examples{
map <- geolibre() |>
  add_3d_tiles("https://example.com/tileset.json", altitude_offset = 20)
stopifnot(map$x$project$layers[[1]]$type == "3d-tiles")
}
