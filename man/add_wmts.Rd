% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_wmts}
\alias{add_wmts}
\title{Add a WMTS layer}
\usage{
add_wmts(
  map,
  url,
  name = "WMTS Layer",
  tile_size = 256,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{url}{A WMTS tile URL template in WMTS REST \code{{z}/{y}/{x}} order, with the
row before the column. This differs from the \code{{z}/{x}/{y}} templates
\code{\link[=add_tile_layer]{add_tile_layer()}} expects.}

\item{name}{Layer name.}

\item{tile_size}{Tile size in pixels.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Add a WMTS layer
}
\examples{
map <- geolibre() |>
  add_wmts("https://example.com/wmts/layer/{z}/{y}/{x}.png", name = "Imagery")
stopifnot(map$x$project$layers[[1]]$type == "wmts")
}
