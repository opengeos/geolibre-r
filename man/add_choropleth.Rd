% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_choropleth}
\alias{add_choropleth}
\title{Add a data-driven choropleth layer}
\usage{
add_choropleth(
  map,
  data,
  column,
  name = "Choropleth",
  class_count = 5,
  colormap = "viridis",
  scheme = c("equal-interval", "quantile"),
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{data}{GeoJSON as a parsed list (a \code{FeatureCollection}, \code{Feature}, or
bare geometry), a JSON string, a file path, an HTTP(S) URL, or an \code{sf}
object. A URL or file is read and inlined into the project, up to a 50 MB
limit; for larger datasets prefer \code{\link[=add_vector]{add_vector()}}, which lets the browser
stream the source.}

\item{column}{Name of the numeric feature property to classify.}

\item{name}{Layer name.}

\item{class_count}{Number of classes, clamped to between 2 and 12.}

\item{colormap}{A color ramp name from \code{\link[=color_ramp_names]{color_ramp_names()}}.}

\item{scheme}{Classification scheme, \code{"equal-interval"} or \code{"quantile"}.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Classifies \code{column} into \code{class_count} numeric ranges and colors each range
from \code{colormap}, producing the same graduated symbology the application's
Style panel builds from the interface.
}
\examples{
counties <- list(
  type = "FeatureCollection",
  features = list(
    list(
      type = "Feature", properties = list(pop = 100),
      geometry = list(type = "Point", coordinates = c(-77, 39))
    ),
    list(
      type = "Feature", properties = list(pop = 900),
      geometry = list(type = "Point", coordinates = c(-76, 40))
    )
  )
)
map <- geolibre() |>
  add_choropleth(counties, column = "pop", colormap = "blues", class_count = 3)
stopifnot(map$x$project$layers[[1]]$style$vectorStyleMode == "graduated")
}
\seealso{
\code{\link[=classify_layer]{classify_layer()}} to symbolize a layer that is already on the map.
}
