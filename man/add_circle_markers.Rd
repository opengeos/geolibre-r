% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_circle_markers}
\alias{add_circle_markers}
\title{Add circle markers}
\usage{
add_circle_markers(
  map,
  points,
  name = "Circle Markers",
  radius = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{points}{Points as a two-column matrix or a data frame of coordinates, a
list of \code{c(longitude, latitude)} pairs or named
\code{list(lng = , lat = , ...)} entries, a point GeoJSON source, or an \code{sf}
object of points.}

\item{name}{Layer name.}

\item{radius}{Optional circle radius in pixels.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
\code{\link[=add_markers]{add_markers()}} with the circle radius surfaced as a named argument.
}
\examples{
map <- geolibre() |>
  add_circle_markers(list(c(-77, 39)), radius = 12, fillColor = "#16a34a")
}
