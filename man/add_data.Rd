% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_data}
\alias{add_data}
\title{Add data, optionally symbolized by a column}
\usage{
add_data(map, data, column = NULL, name = "Data", ...)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{data}{GeoJSON as a parsed list (a \code{FeatureCollection}, \code{Feature}, or
bare geometry), a JSON string, a file path, an HTTP(S) URL, or an \code{sf}
object. A URL or file is read and inlined into the project, up to a 50 MB
limit; for larger datasets prefer \code{\link[=add_vector]{add_vector()}}, which lets the browser
stream the source.}

\item{column}{Optional numeric property to drive graduated symbology.}

\item{name}{Layer name.}

\item{...}{Forwarded to \code{\link[=add_choropleth]{add_choropleth()}} or \code{\link[=add_geojson]{add_geojson()}}.}
}
\value{
The modified widget.
}
\description{
With \code{column} supplied this is \code{\link[=add_choropleth]{add_choropleth()}}; without it, \code{\link[=add_geojson]{add_geojson()}}.
Provided for parity with the Python API.
}
\examples{
point <- list(type = "Point", coordinates = c(-77, 39))
map <- geolibre() |> add_data(point, name = "Point")
}
