% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html.R
\name{to_html}
\alias{to_html}
\title{Export a map as a standalone HTML page}
\usage{
to_html(
  map,
  path = NULL,
  title = "GeoLibre Map",
  width = "100\%",
  height = "800px",
  app_url = getOption("geolibre.app_url", "https://web.geolibre.app/")
)
}
\arguments{
\item{map}{A GeoLibre widget or project list.}

\item{path}{Optional output path. When supplied the page is written there and
\code{path} is returned invisibly; otherwise the HTML is returned as a string.}

\item{title}{The exported page's \code{title}.}

\item{width}{CSS width of the embedded map, for example \code{"100\%"} or \code{"800px"}.}

\item{height}{CSS height of the embedded map.}

\item{app_url}{Base URL of the GeoLibre application to embed. Defaults to the
hosted viewer so the export stays portable; pass a self-hosted deployment
URL to pin a specific version.}
}
\value{
The HTML string, or \code{path} invisibly when \code{path} is supplied.
}
\description{
The page embeds the GeoLibre application in an \code{iframe} and injects the
project into it over the same \code{postMessage} bridge the widget uses, so it
renders the map as configured. Unlike the widget it needs no running R
session; by default it loads the hosted application over the network so the
file stays portable.
}
\details{
Credentials are stripped from the inlined project on the way out, as
\code{\link[=save_project]{save_project()}} does.
}
\examples{
map <- geolibre() |> add_marker(-77.0369, 38.9072, name = "DC")
html <- to_html(map, title = "Washington, DC")
substr(html, 1, 15)

path <- tempfile(fileext = ".html")
to_html(map, path)
}
\seealso{
\code{\link[=save_project]{save_project()}} to write the project itself
}
