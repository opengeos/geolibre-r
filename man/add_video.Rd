% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-raster.R
\name{add_video}
\alias{add_video}
\title{Add a georeferenced video layer}
\usage{
add_video(
  map,
  urls,
  coordinates,
  name = "Video",
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{urls}{One video URL, or several as format fallbacks such as MP4 then
WebM. URLs must be \verb{https://}, since the browser's media policy blocks
plain HTTP.}

\item{coordinates}{Four \code{c(longitude, latitude)} corners in top-left,
top-right, bottom-right, bottom-left order, given as a list of pairs or a
four-row matrix.}

\item{name}{Layer name.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
Add a georeferenced video layer
}
\examples{
map <- geolibre() |>
  add_video(
    "https://example.com/clip.mp4",
    coordinates = list(
      c(-77.1, 39.0), c(-77.0, 39.0), c(-77.0, 38.9), c(-77.1, 38.9)
    )
  )
stopifnot(map$x$project$layers[[1]]$type == "video")
}
