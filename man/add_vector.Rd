% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layers-vector.R
\name{add_vector}
\alias{add_vector}
\title{Add a vector dataset from a URL or local file}
\usage{
add_vector(
  map,
  data,
  name = "Vector",
  render_mode = c("geojson", "tiles"),
  data_format = NULL,
  source_layer = NULL,
  picker = NULL,
  ingest_mode = NULL,
  style = list(),
  visible = TRUE,
  opacity = 1,
  ...
)
}
\arguments{
\item{map}{A GeoLibre widget.}

\item{data}{A dataset URL, a local file path, or an \code{sf} object.}

\item{name}{Layer name.}

\item{render_mode}{\code{"geojson"} to load into a GeoJSON source, or \code{"tiles"} to
stream as vector tiles. Remote URLs only.}

\item{data_format}{Optional format hint for remote URLs, for example
\code{"parquet"} or \code{"flatgeobuf"}. The control auto-detects when omitted.}

\item{source_layer}{Optional layer or table name inside a multi-layer
container such as a GeoPackage.}

\item{picker}{Optional toggle for the control's feature-inspection popup.}

\item{ingest_mode}{Optional ingest strategy, \code{"table"} or \code{"stream"}.}

\item{style}{Named list of GeoLibre style overrides such as \code{fillColor},
\code{strokeColor}, and \code{strokeWidth}.}

\item{visible}{Whether the layer is initially visible.}

\item{opacity}{Layer opacity from zero to one.}

\item{...}{Additional style overrides given as named arguments, merged into
\code{style}. \code{add_geojson(map, data, fillColor = "red")} and
\code{add_geojson(map, data, style = list(fillColor = "red"))} are equivalent.}
}
\value{
The modified widget.
}
\description{
A remote URL is handed to the application's in-browser vector control, so any
format it reads (GeoParquet, FlatGeobuf, zipped Shapefile, GeoPackage,
GeoJSON, KML, ...) streams without being inlined in the project. A local file
is read with \code{sf} and inlined as GeoJSON, since the browser cannot reach a
file on this machine.
}
\examples{
map <- geolibre() |>
  add_vector("https://example.com/data.parquet", name = "Parcels")
stopifnot(map$x$project$layers[[1]]$metadata$sourceKind == "maplibre-gl-vector")
}
